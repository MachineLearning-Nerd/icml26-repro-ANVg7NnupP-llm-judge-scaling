# Scored claims

1. When reward is not too different from teacher, generalization error decreases monotonically with increasing inference-time samples `k`.
2. Substantial reward misspecification induces a finite optimal `k` beyond which more sampling increases generalization error.
3. With best-of-`k` limit and teacher as reward, generalization error decays as `Theta(1/k^2)`.

