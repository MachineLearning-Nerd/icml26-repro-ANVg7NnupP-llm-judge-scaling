# Claim 5 method

The Remark 5 expression is factorized as `C(alpha)*k^-2`. For isotropic
covariance and `||w||^2=d`, the ridge is reconstructed from the closed form and
its implicit deterministic-equivalent equation. An independent Brent root and
central differences in `log(n)` check the analytic training derivative at five
prespecified `alpha` values. Both dimensionless smallness assumptions are
audited. A deliberately out-of-domain case must reject inference dominance.
