# Claim 3 limitations and deviations

This directly verifies the finite-optimum/non-monotonic consequence used by the
live judge. It does not independently certify the separate asymptotic threshold
and `k*` approximation formulas; those remain an explicitly scoped limitation.
