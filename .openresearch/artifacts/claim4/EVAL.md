# Claim 4 evaluation

Verdict: **VERIFIED**.

The symbolic certificate establishes the unique optimizer of the stated
second-order expansion. Ten eligible `t>=20` exact calibrations have median
4.4303% and maximum 8.5676% relative error. The independent Monte Carlo checker
has maximum `|z|=0.443`. Omitting `(1-2/k)` incurs 186.59% error, and `k=2` is
rejected. Every fail-closed check passed.
