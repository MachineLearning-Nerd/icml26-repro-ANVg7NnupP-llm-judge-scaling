# Claim 4 limitations and deviations

The proof certificate addresses the exact truncated-expansion formula. Exact
finite-temperature optimization is a calibration, not a replacement theorem.
The qualitative domain `t>>1` is operationalized transparently as `t>=20`;
lower-temperature rows remain visible but do not enter acceptance.
