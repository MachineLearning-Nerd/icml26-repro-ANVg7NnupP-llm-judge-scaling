# Claim 1 method

An independent survival-function integral evaluates the minimum squared
Gaussian residual for four teacher offsets and 13 fixed `k` values through
16,384. A log-log tail fit checks the exponent; `delta*k^2` checks the exact
leading coefficient. This calculation does not reuse the finite-temperature
quadrature used for Claims 2 and 3.
